# API Backend URL (Leave empty or omit to use Vite dev proxy in local development)
# For production deployment (e.g. Render/Railway backend with Vercel frontend), set this to your backend URL:
# VITE_API_URL=https://your-backend-api.onrender.com
VITE_API_URL=
